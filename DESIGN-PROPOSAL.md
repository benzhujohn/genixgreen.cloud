# GENIXGREEN App 下载站 — UI 框架方案（待确认版 v0.1）

> 交付日期：2026-09-11　｜　状态：**框架预览，等您确认后执行**
> 预览文件：`genixgreen-app/index.html`（可直接双击打开看效果）

---

## 一、现有站点诊断（先说为什么要重做）

我抓取了 `https://www.genixgreen.cloud/` 的源码逐行核对，结论：**这是一套二手 Bootstrap 模板改的，而且没改完。**

| 问题 | 证据 |
|---|---|
| **主色仍是模板默认紫** | CSS 里 `--bs-primary: #5954cb`（紫色），与 logo 实际用色 `#009944` 绿 / `#F08300` 橙 **完全不符**。全站按钮是紫色的。 |
| **文案残留模板 demo 内容** | 页面写着 "Page Load Details (time, size, number of requests)"、"Waterfall, Video and Report History"、"Various Analysis Options" —— 这些是**网页性能监控模板**的示例文案，和锂电池毫无关系，还挂在线上。 |
| **结构过时** | Bootstrap 4 语法混 Bootstrap 5 类名（`bootstrap.bundle.min.js` + `ml-auto`），依赖 FontAwesome 5.7 CDN + Google Fonts 外部字体，国内加载不稳。 |
| **没有二维码** | 只有一个「Google Play」按钮，实际指向 `.apk` 直链（不是 Google Play），名不副实；也没有扫码入口 —— 桌面端用户没法下载。 |
| **无 SEO / 无响应式优化** | 没有结构化数据、没有 OG 图、没有 hreflang。 |

**结论：重建比修更快，且成本更低。**

### 顺带挖到的现成资产（可以直接复用）

| 项目 | 值 |
|---|---|
| Android APK 直链 | `https://www.genixgreen.cloud/download/android/genixgreen.apk`　（27 MB，最近更新 2026-04-22） |
| iOS App Store | `https://apps.apple.com/cn/app/genixgreen/id6479182549` |
| SaaS 网页端 | `https://mana.genixgreen.cloud/` |
| Logo（矢量） | `https://www.genixgreen.cloud/img/logo.svg`（已下载到项目里） |
| 应用截图 | `https://www.genixgreen.cloud/img/mobile.png`（单张 iPhone X mockup） |
| 联系方式 | app@zwaynenergy.com　/　+86 138 9298 1183 |

> ⚠️ 需要您确认：现有页脚署名是 **Dongguan ZWAYN New Energy Co., Ltd.**，和品牌名 GENIXGREEN 不一致。新站页脚到底写哪个主体？

---

## 二、目标与信息架构

**一句话目标**：让访客在 5 秒内明白「这是什么 App / 能干什么」并完成下载，桌面扫码、手机直接点。

信息架构（自上而下 11 个区块，单页滚动，锚点导航）：

```
① 导航栏        Logo · 功能 · 截图 · 步骤 · 兼容 · FAQ · [下载按钮]
② Hero 首屏     大标题 + 双下载按钮 + 手机模拟界面 + 二维码卡片 ★核心区
③ 数据条        装机量 / 评分 / 支持容量 / 语种（4 格，可整块关闭）
④ 功能矩阵      6 张卡片：实时监控·远程关机·充电分析·告警·多设备·报表
⑤ 应用截图      横滑图库 6 张（手机内屏占位）
⑥ 三步上手      下载注册 → 添加电池 → 监控控制
⑦ 兼容型号      5 / 10 / 16 kWh 壁挂全系 + 视频位
⑧ 常见问题      6 条手风琴（免费吗 / 怎么连接 / 断网能用吗 / 系统版本 / 为什么有 APK / 怎么找售后）
⑨ 转化区        深绿大横幅 + 第二组二维码 ★二次转化
⑩ 页脚          品牌 / 快捷链接 / 联系方式 + ICP 备案
⑪ 移动端吸底条   滚动 520px 后出现，常驻下载入口
```

---

## 三、视觉系统

| 令牌 | 值 | 说明 |
|---|---|---|
| 主色 Brand Green | `#009944` | **直接取自 logo 实际用色**，不是拍脑袋定的 |
| 主色深阶 | `#008A3D` / `#006F31` | 悬停、渐变收尾 |
| 强调色 Accent | `#F08300` | 取自 logo 橙色，用于告警类图标、占位标记 |
| 品牌浅底 | `#EAF7F0` | 卡片图标底、悬停底色 |
| 正文墨色 | `#0B1A12` / `#1F2E26` | 带一点绿调的近黑，比纯黑高级 |
| 分割线 | `#E2EAE5` | 极浅绿灰 |
| 背景 | `#FFFFFF` / `#F5F8F6` | 冷暖交替分节 |

- **字体**：Inter + 系统字体栈（`-apple-system / Segoe UI / PingFang SC`），**不依赖 Google Fonts CDN**，国内秒开。
- **圆角**：卡片 24px，按钮全圆角（pill），手机 40px。
- **阴影**：三档极浅阴影（`rgba(11,26,18,.06)` 系），不用重投影，保持干净。
- **动效**：仅 4 处 —— 卡片悬停上浮 4px、滚动淡入、手机旁数据浮标 6s 轻浮动、识别系统后推荐按钮呼吸光圈。全部尊重 `prefers-reduced-motion`。
- **预览画面里的橙色小标签**（如「QR 待填」「截图待填」）是**开发期占位标记**，上线前一键删除，方便您对照验收。

---

## 四、几个真正有用的交互（不是花架子）

| 功能 | 作用 |
|---|---|
| **系统自动识别** | 检测到 iOS 就高亮 App Store 按钮、安卓就高亮下载按钮，另一个降级为次要样式。App 下载站的转化率关键点。 |
| **二维码双份** | Hero 区 + 底部转化区各一份，桌面用户滚动到哪都能扫。 |
| **一键复制链接** | 二维码下方两个按钮，复制后可直接贴到 WhatsApp / LINE / 微信发客户。 |
| **微信环境提示** | 检测到 `MicroMessenger`，弹层提示「右上角 → 在浏览器打开」，解决微信内无法装包的经典坑。 |
| **空链接自动降级** | 您还没给的链接，按钮自动变成「Coming soon」灰态并弹提示，**不会出现点进去 404 的尴尬**。 |
| **移动端吸底条** | 手机端滚过首屏后底部常驻双按钮，随时可点。 |

---

## 五、技术选型与目录结构

**纯静态站**：原生 HTML + CSS + JS，零框架、零构建、零依赖。
理由：一个下载页不需要 React/Vue；静态站可托管 GitHub Pages 免费、秒开、不用服务器，也方便您自己改文案。

```
genixgreen-app/
├── index.html                    # 单页
├── assets/
│   ├── css/style.css             # 设计令牌 + 全部样式（含响应式断点 1040/860/620）
│   ├── js/
│   │   ├── config.js             # ★ 唯一待填区：链接 / 二维码 / 版本号 / 联系信息
│   │   └── main.js               # 注入 + 交互
│   └── img/
│       ├── logo.svg              # 品牌 logo（已从官网下载）
│       ├── favicon.svg
│       ├── qr-android.svg        # 安卓二维码占位
│       └── qr-ios.svg            # 苹果二维码占位
├── DESIGN-PROPOSAL.md            # 本文件
└── README.md
```

**核心设计**：所有会变的东西都抽到 `config.js` 一个文件里。您给资料后，我只改这一个文件，全站文案、链接、二维码自动同步。

响应式断点：≥1040px 双栏 / 860px 以下收起导航出吸底条 / 620px 以下全单栏。

---

## 六、需要您提供的资料（★ 待填清单）

| # | 项目 | 现状 | 优先级 |
|---|---|---|---|
| 1 | **安卓二维码图片** | 占位 | 🔴 必给 |
| 2 | **iOS 二维码图片** | 占位 | 🔴 必给 |
| 3 | **安卓下载链接** | 已找到官网 APK 直链，是否沿用？ | 🔴 必给 |
| 4 | **iOS 下载链接** | 已找到 App Store 链接，是否沿用？ | 🔴 必给 |
| 5 | App 版本号 / 更新日期 / 安装包大小 | 占位 v2.4.0 / Sep 2026 / 27 MB | 🟡 建议给 |
| 6 | App 界面截图 5–6 张 | 占位 | 🟡 建议给 |
| 7 | 产品演示视频（或 YouTube 链接） | 占位 | 🟢 可选 |
| 8 | 数据条的 4 个数字（装机量/评分/语种） | 占位待核 | 🟢 可选 |
| 9 | 页脚署名主体（GENIXGREEN 还是 ZWAYN） | 待确认 | 🔴 必答 |

> 图片请给我**正方形 PNG，≥600×600px，白底**，这样嵌进卡片不会变形。

---

## 七、GitHub 部署方案

1. 新建仓库 `genixgreen-app`（建议公开，GitHub Pages 免费版需要公开仓库）
2. 推 `main` 分支 → Settings → Pages → Source 选 `main / (root)`
3. 得到地址：`https://<你的账号>.github.io/genixgreen-app/`
4. **绑自定义域**（推荐）：加 `CNAME` 文件写 `app.genixgreen.com`，再去域名 DNS 加一条 `CNAME → <账号>.github.io`，开启 HTTPS 即可
5. 附赠：加 `robots.txt` + `sitemap.xml` + JSON-LD 结构化数据（`SoftwareApplication` 类型，让 Google 直接展示评分和下载按钮）

> 环境检查：本机 `git 2.55.0` 可用，但 `gh` CLI **未安装**、git 用户名/邮箱**未配置**。推送时我会走 **GitHub MCP 连接器**（已连接），或者您给我一个仓库地址 + Personal Access Token。**如果要绑 `app.genixgreen.com`，请一并告诉我 DNS 由谁管理。**

---

## 八、请您确认这 5 个决策点

| # | 问题 | 我的建议 |
|---|---|---|
| **A** | 配色走「品牌绿主色 + 浅色背景」还是「深色科技风」？ | **推荐浅色绿**（已按此做预览）。产品是家用储能，浅色更亲和，也符合您一贯的极简审美。 |
| **B** | 语言：只做英文？加泰文？还是英文+泰文切换？ | 现有站是纯英文。若主推泰国，建议**英文为主 + 泰文切换**。 |
| **C** | 部署到 GitHub Pages 独立域名 `app.genixgreen.com`，还是挂在现有 `genixgreen.cloud` 下？ | 独立子域，干净且不影响现有站。 |
| **D** | 现有官网那个紫色版本，是**替换**还是并存？ | 建议最终替换，先把新站跑起来验证。 |
| **E** | 页脚主体署名用哪个？ | 需您给答案（见第六节 #9）。 |

---

## 九、下一步

1. 您打开 `genixgreen-app/index.html` 看框架（**注意：现在按钮是灰的「Coming soon」态，属预期，因为链接还没给**）
2. 回复上面 A–E 五个决策 + 第六节的资料
3. 我执行：替换素材 → 接链接 → 补 SEO/结构化数据 → 推送 GitHub → 开 Pages → 给您线上地址

---

*方案文档 v0.1 · 框架已可运行 · 等您拍板*
